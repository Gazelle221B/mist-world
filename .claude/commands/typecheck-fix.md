---
description: Run the project's typechecker
---

# Typecheck and Fix

This command runs `npm run typecheck` to verify the codebase's TypeScript definitions.
If the command fails, it automatically reviews the errors and applies the necessary fixes to make the codebase compile successfully.

If no argument is given, it checks the whole codebase. If a file is given, it pays special attention to that file's errors.
